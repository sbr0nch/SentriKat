# Force Unix line endings for shell scripts and config templates
# Prevents CRLF issues when cloning on Windows and running in Linux containers
*.sh text eol=lf
*.conf text eol=lf
*.template text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
