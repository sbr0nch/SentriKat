apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentrikat.fullname" . }}
  labels:
    {{- include "sentrikat.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentrikat.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
